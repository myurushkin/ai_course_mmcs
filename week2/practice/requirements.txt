# Практика недели 2. В Colab всё это ставится первой ячейкой.
openai-agents>=0.22  # фреймворк практики: Agent, Runner, function_tool
openai>=1.50         # клиент к любому OpenAI-совместимому сервису
pydantic>=2          # типы аргументов инструментов и форма ответа агента
